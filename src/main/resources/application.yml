server:
  port:8083

spring:
  application:
      name:demo

  datasource:
    url: jdbc:postgresql://localhost:5432/nitereGitAction
    username: postgres
    password: Jie@140305
    driver-class-name: org.postgresql.Driver
    hikari:
      minimum-idle: 5
      maximum-pool-size: 20
      auto-commit: true
      idle-timeout: 60000
      pool-name: DateSourceHikariCP
      max-lifetime: 1800000
      connection-timeout: 30000
      connection-test-query: SELECT 1

  jpa:
     hibernate:
        ddl-auto: update
     show-sql: true
     properties:
        hibernate:
          dialect: org.hibernate.dialect.PostgreSQLDialect
          format_sql: true